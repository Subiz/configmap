
stripekey: stripe.apikey
/tmp/testconfigmap: s2.password

# configmap -config config1.yaml -format bash configmap.yaml
# output:
#
# mkdir -p /tmp && printf "%s" "2" > /tmp/testconfigmap
# export stripekey="1"
